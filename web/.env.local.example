# 백엔드 API 베이스 URL (미설정 시 http://127.0.0.1:8010 사용)
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8010

# 웹 접근 비밀번호. 설정하면 로그인 게이트가 켜지고, 비우면 게이트가 열린다(로컬 개발).
# 서버 사이드 전용(NEXT_PUBLIC_ 아님) — 브라우저 번들에 노출되지 않는다.
LOGIN_PASSWORD=
