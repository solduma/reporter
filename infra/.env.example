# infra/docker-compose.yml 용 변수. 실제 값은 infra/.env (gitignore) 에 둔다.
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
# reporter-worker(스케줄러)가 GLM 호출에 사용
OLLAMA_HOST=
OLLAMA_API_KEY=
SUMMARY_MODEL=
INSIGHT_MODEL=
